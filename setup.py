from setuptools import setup

setup(name='token_dropping',
      version='0.1',
      description='token_dropping',
      url='https://github.com/yujiepan-work/token-dropping',
      author='Pan, Yujie',
      author_email='yujie.pan@intel.com',
      packages=['token_dropping'],
      zip_safe=False)
